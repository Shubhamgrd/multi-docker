sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t shubham465/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true shubham465/react-test npm test

after_success:
  - docker build -t shubham465/multi-client ./client
  - docker build -t shubham465/multi-nginx ./nginx
  - docker build -t shubham465/multi-server ./server
  - docker build -t shubham465/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push shubham465/multi-client
  - docker push shubham465/multi-nginx
  - docker push shubham465/multi-server
  - docker push shubham465/multi-worker
