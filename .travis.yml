sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t shubham/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true shubham/react-test npm test

after_success:
  - docker build -t shubham/multi-client ./client
  - docker build -t shubham/multi-nginx ./nginx
  - docker build -t shubham/multi-server ./server
  - docker build -t shubham/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push shubham/multi-client
  - docker push shubham/multi-nginx
  - docker push shubham/multi-server
  - docker push shubham/multi-worker
